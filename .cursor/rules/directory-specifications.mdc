---
description: 
globs: 
alwaysApply: true
---
# Directory specifications

```
src/
├── assets/                 # 静态资源
│   ├── images/             # 图片资源
│   └── styles/             # 全局样式
├── components/             # 基础组件
│   ├── common/             # 全局通用组件（ PascalCase 命名）
│   └── [module-name]/      # 按业务模块划分的组件
├── composables/            # 组合式 API
│   ├── common/             # 全局通用组合式 API（ camelCase 命名）
│   └── [module-name]/      # 按业务模块划分的组合式 API
├── views/                  # 页面级组件
│   ├── [module-name]/      # 按业务模块划分的页面（ PascalCase 命名）
├── router/                 # 路由配置
│   ├── [module-name].ts    # 按业务模块划分的路由（ camelCase 命名）
│   └── index.ts            # 路由主文件
├── store/                  # 状态管理
│   ├── modules/            # Pinia 模块化存储（按功能拆分）
│   └── index.ts            # 状态管理主文件
├── api/                    # API服务层
│   ├── [module-name].ts    # 按业务模块划分的 API 接口
├── utils/                  # 工具函数
│   ├── [module-name].ts    # 按业务模块划分的工具函数
├── types/                  # 类型定义
│   ├── [module-name].d.ts  # 按业务模块划分的类型定义
│   └── global.d.ts         # 全局类型声明
└── App.vue                 # 根组件
```